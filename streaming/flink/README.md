# Streaming layer (placeholder)

This folder will contain Flink jobs for near-real-time **risk decisions** and **case routing** (speed layer).

Batch layer is under `batch/spark/`.

Suggested layout:

- `jobs/` Flink jobs (Java) per pipeline step
- `connectors/` Kafka / DB connectors (if needed)

